# WebAuthn relying-party configuration.
# In dev these defaults match `dx serve` on http://localhost:8080.
# In production set them to your real domain (and serve over HTTPS).
WEBAUTHN_RP_ID=localhost
WEBAUTHN_RP_ORIGIN=http://localhost:8080
WEBAUTHN_RP_NAME=PhotoVault
